package algorithms

type SelectionSort struct{}

func (s SelectionSort) Sort(done, changed chan bool, data *[]float64) {
	// var key float64
	// for _, v := range <-data {
	// 	key = v
	// }
}
